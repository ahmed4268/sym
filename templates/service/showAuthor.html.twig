<ul>

    <li>
        <img src="{{ author.picture }}" alt="{{ author.username }}" width="100" height="100">
        <strong>{{ author.username|upper }}</strong> - {{ author.email }} - Nombre de livres: {{ author.nb_books }}
    </li>
</ul>